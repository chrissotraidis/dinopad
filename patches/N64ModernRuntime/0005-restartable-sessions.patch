diff --git a/librecomp/src/recomp.cpp b/librecomp/src/recomp.cpp
index ea17675..8232141 100644
--- a/librecomp/src/recomp.cpp
+++ b/librecomp/src/recomp.cpp
@@ -539,8 +539,6 @@ void ultramodern::quit() {
 void ultramodern::quit() {
     exited.store(true);
     GameStatus desired = GameStatus::None;
     game_status.compare_exchange_strong(desired, GameStatus::Quit);
     game_status.notify_all();
-    std::lock_guard<std::mutex> lock(current_game_mutex);
-    current_game.reset();
 }
@@ -730,6 +730,8 @@ bool recomp::flashram_allowed() {
 }
 
 void recomp::start(const recomp::Configuration& cfg) {
+    exited.store(false);
+
     project_version = cfg.project_version;
     recomp::check_all_stored_roms();
 
@@ -825,8 +827,20 @@ void recomp::start(const recomp::Configuration& cfg) {
 
     game_thread.join();
     ultramodern::join_event_threads();
+    ultramodern::join_timer_thread();
     ultramodern::join_thread_cleaner_thread();
     ultramodern::join_saving_thread();
+    game_status.store(GameStatus::None);
+    {
+        std::lock_guard<std::mutex> lock(current_game_mutex);
+        current_game.reset();
+    }
+
+    {
+        std::lock_guard lock{mod_context_mutex};
+        mod_context->unload_mods();
+        mod_context->close_mods();
+    }
     
     // Free rdram.
     bool free_failed;
diff --git a/librecomp/src/overlays.cpp b/librecomp/src/overlays.cpp
index 04fa31e..dc3ccfc 100644
--- a/librecomp/src/overlays.cpp
+++ b/librecomp/src/overlays.cpp
@@ -277,6 +277,9 @@ extern "C" void unload_overlays(int32_t ram_addr, uint32_t size) {
 
 void recomp::overlays::init_overlays() {
     func_map.clear();
+    loaded_sections.clear();
+    code_sections_by_rom.clear();
+    free(section_addresses);
     section_addresses = (int32_t *)calloc(sections_info.total_num_sections, sizeof(int32_t));
 
     // Sort the executable sections by rom address
@@ -320,7 +323,9 @@ bool recomp::overlays::get_func_entry_by_section_index_function_offset(uint16_t
 
 void recomp::overlays::register_manual_patch_symbols(const ManualPatchSymbol* manual_patch_symbols) {
     for (size_t i = 0; manual_patch_symbols[i].func != nullptr; i++) {
-        if (!manual_patch_symbols_by_vram.emplace(manual_patch_symbols[i].ram_addr, manual_patch_symbols[i].func).second) {
+        auto [existing, inserted] = manual_patch_symbols_by_vram.emplace(
+            manual_patch_symbols[i].ram_addr, manual_patch_symbols[i].func);
+        if (!inserted && existing->second != manual_patch_symbols[i].func) {
             printf("Duplicate manual patch symbol address: %08X\n", manual_patch_symbols[i].ram_addr);
             ultramodern::error_handling::message_box("Duplicate manual patch symbol address (syms.ld)!");
             assert(false && "Duplicate manual patch symbol address (syms.ld)!");
diff --git a/ultramodern/include/ultramodern/ultramodern.hpp b/ultramodern/include/ultramodern/ultramodern.hpp
index 57db6f6..ade5eb8 100644
--- a/ultramodern/include/ultramodern/ultramodern.hpp
+++ b/ultramodern/include/ultramodern/ultramodern.hpp
@@ -36,6 +36,7 @@ void preinit(RDRAM_ARG renderer::WindowHandle window_handle);
 void init_saving(RDRAM_ARG1);
 void init_events(RDRAM_ARG renderer::WindowHandle window_handle);
 void init_timers(RDRAM_ARG1);
+void join_timer_thread();
 void init_thread_cleanup();
 
 // Saving
@@ -75,6 +76,7 @@ struct MessageQueueControl {
 void set_message_queue_control(const MessageQueueControl& mqc);
 void enqueue_external_message_src(PTR(OSMesgQueue) mq, OSMesg msg, bool jam, EventMessageSource src);
 void enqueue_external_message(PTR(OSMesgQueue) mq, OSMesg msg, bool jam, bool requeue_if_blocked);
+void reset_external_messages();
 void wait_for_external_message(RDRAM_ARG1);
 void wait_for_external_message_timed(RDRAM_ARG1, u32 millis);
 
diff --git a/ultramodern/src/events.cpp b/ultramodern/src/events.cpp
index 1333133..218e9cd 100644
--- a/ultramodern/src/events.cpp
+++ b/ultramodern/src/events.cpp
@@ -574,6 +574,37 @@ void ultramodern::send_si_message() {
 void ultramodern::init_events(RDRAM_ARG ultramodern::renderer::WindowHandle window_handle) {
     moodycamel::LightweightSemaphore gfx_thread_ready;
     moodycamel::LightweightSemaphore task_thread_ready;
+
+    // Messages retain guest queue addresses, so none may cross an RDRAM
+    // teardown/restart boundary.
+    ultramodern::reset_external_messages();
+    total_vis = 0;
+    renderer_setup_result.store(ultramodern::renderer::SetupResult::Success);
+    renderer_chosen_api.store(ultramodern::renderer::GraphicsApi::Auto);
+
+    {
+        std::lock_guard lock{events_context.message_mutex};
+        events_context.vi.cur_state = 0;
+        events_context.vi.field = 0;
+        events_context.vi.states[0] = ViState{};
+        events_context.vi.states[1] = ViState{};
+        events_context.vi.regs = ultramodern::renderer::ViRegs{};
+        events_context.vi.update_screen_regs = ultramodern::renderer::ViRegs{};
+        events_context.sp.mq = NULLPTR;
+        events_context.sp.msg = (OSMesg)0;
+        events_context.dp.mq = NULLPTR;
+        events_context.dp.msg = (OSMesg)0;
+        events_context.ai.mq = NULLPTR;
+        events_context.ai.msg = (OSMesg)0;
+        events_context.si.mq = NULLPTR;
+        events_context.si.msg = (OSMesg)0;
+    }
+
+    Action stale_action{UpdateConfigAction{}};
+    while (events_context.action_queue.try_dequeue(stale_action)) {}
+    OSTask* stale_task = nullptr;
+    while (events_context.sp_task_queue.try_dequeue(stale_task)) {}
+
     events_context.rdram = rdram;
     events_context.sp.gfx_thread = std::thread{ gfx_thread_func, rdram, &gfx_thread_ready, window_handle };
     events_context.sp.task_thread = std::thread{ task_thread_func, rdram, &task_thread_ready };
diff --git a/ultramodern/src/mesgqueue.cpp b/ultramodern/src/mesgqueue.cpp
--- a/ultramodern/src/mesgqueue.cpp
+++ b/ultramodern/src/mesgqueue.cpp
@@ -34,6 +34,11 @@ void ultramodern::enqueue_external_message(PTR(OSMesgQueue) mq, OSMesg msg, bool
     external_messages.enqueue({mq, msg, jam, requeue_if_blocked});
 }
 
+void ultramodern::reset_external_messages() {
+    QueuedMessage stale_message{};
+    while (external_messages.try_dequeue(stale_message)) {}
+}
+
 bool do_send(RDRAM_ARG PTR(OSMesgQueue) mq_, OSMesg msg, bool jam, bool block);
 
 void dequeue_external_messages(RDRAM_ARG1) {
diff --git a/ultramodern/src/threads.cpp b/ultramodern/src/threads.cpp
index 812c556..8d0f952 100644
--- a/ultramodern/src/threads.cpp
+++ b/ultramodern/src/threads.cpp
@@ -2,6 +2,9 @@
 #include <thread>
 #include <cassert>
 #include <string>
+#include <mutex>
+#include <unordered_set>
+#include <vector>
 
 #include "ultramodern/ultra64.h"
 #include "ultramodern/ultramodern.hpp"
@@ -15,6 +18,9 @@
 #endif
 
 static ultramodern::threads::callbacks_t threads_callbacks;
+static std::mutex active_thread_contexts_mutex;
+static std::unordered_set<UltraThreadContext*> active_thread_contexts;
+extern std::atomic_bool exited;
 
 void ultramodern::threads::set_callbacks(const callbacks_t& callbacks) {
     threads_callbacks = callbacks;
@@ -148,6 +154,9 @@ void ultramodern::set_native_thread_priority(ThreadPriority pri) {}
 
 void wait_for_resumed(RDRAM_ARG UltraThreadContext* thread_context) {
     thread_context->running.wait();
+    if (exited) {
+        throw ultramodern::thread_terminated{};
+    }
     // If this thread's context was replaced by another thread or deleted, destroy it again from its own context.
     // This will trigger thread cleanup instead.
     if (TO_PTR(OSThread, ultramodern::this_thread())->context != thread_context) {
@@ -203,6 +212,11 @@ static void _thread_func(RDRAM_ARG PTR(OSThread) self_, PTR(thread_func_t) entry
     } catch (ultramodern::thread_terminated& terminated) {
     }
 
+    if (exited) {
+        ultramodern::cleanup_thread(thread_context);
+        return;
+    }
+
     // Make sure the thread wasn't replaced or destroyed before it was started.
     if (self->context == thread_context) {
         debug_printf("[Thread] Thread started: %d\n", self->id);
@@ -216,6 +230,11 @@ static void _thread_func(RDRAM_ARG PTR(OSThread) self_, PTR(thread_func_t) entry
         debug_printf("[Thread] Thread destroyed before being started: %d\n", self->id);
     }
 
+    if (exited) {
+        ultramodern::cleanup_thread(thread_context);
+        return;
+    }
+
     // Check if the thread hasn't been destroyed or replaced. If so, then the thread terminated or destroyed itself,
     // so mark this thread as destroyed and run the next queued thread.
     if (self->context == thread_context) {
@@ -259,7 +278,16 @@ extern "C" void osCreateThread(RDRAM_ARG PTR(OSThread) t_, OSId id, PTR(thread_f
     // Pass the context as an argument to the thread function to ensure that it can't get cleared before the thread captures its value.
     UltraThreadContext* context = new UltraThreadContext{};
     t->context = context;
-    context->host_thread = std::thread{_thread_func, PASS_RDRAM t_, entrypoint, arg, t->context};
+    {
+        std::lock_guard lock{active_thread_contexts_mutex};
+        if (exited) {
+            t->context = nullptr;
+            delete context;
+            throw ultramodern::thread_terminated{};
+        }
+        active_thread_contexts.insert(context);
+        context->host_thread = std::thread{_thread_func, PASS_RDRAM t_, entrypoint, arg, context};
+    }
 
     // Wait until the thread is initialized to indicate that it's ready to be started.
     context->initialized.wait();
@@ -339,7 +367,15 @@ PTR(OSThread) ultramodern::this_thread() {
 
 static std::thread thread_cleaner_thread;
 static moodycamel::BlockingConcurrentQueue<UltraThreadContext*> deleted_threads{};
-extern std::atomic_bool exited;
+
+static void delete_thread_context(UltraThreadContext* context) {
+    context->host_thread.join();
+    {
+        std::lock_guard lock{active_thread_contexts_mutex};
+        active_thread_contexts.erase(context);
+    }
+    delete context;
+}
 
 void thread_cleaner_func() {
     using namespace std::chrono_literals;
@@ -348,8 +384,7 @@ void thread_cleaner_func() {
         if (deleted_threads.wait_dequeue_timed(to_delete, 10ms)) {
             debug_printf("[Cleanup] Deleting thread context %p\n", to_delete);
 
-            to_delete->host_thread.join();
-            delete to_delete;
+            delete_thread_context(to_delete);
         }
     }
 }
@@ -364,4 +399,28 @@ void ultramodern::cleanup_thread(UltraThreadContext *cur_context) {
 
 void ultramodern::join_thread_cleaner_thread() {
     thread_cleaner_thread.join();
+
+    std::vector<UltraThreadContext*> remaining_contexts;
+    {
+        std::lock_guard lock{active_thread_contexts_mutex};
+        remaining_contexts.assign(active_thread_contexts.begin(), active_thread_contexts.end());
+        for (UltraThreadContext* context : remaining_contexts) {
+            context->running.signal();
+        }
+    }
+
+    for (UltraThreadContext* context : remaining_contexts) {
+        context->host_thread.join();
+    }
+
+    UltraThreadContext* to_delete = nullptr;
+    while (deleted_threads.try_dequeue(to_delete)) {}
+
+    {
+        std::lock_guard lock{active_thread_contexts_mutex};
+        active_thread_contexts.clear();
+    }
+    for (UltraThreadContext* context : remaining_contexts) {
+        delete context;
+    }
 }
diff --git a/ultramodern/src/timer.cpp b/ultramodern/src/timer.cpp
index ee26c45..70d0f0a 100644
--- a/ultramodern/src/timer.cpp
+++ b/ultramodern/src/timer.cpp
@@ -44,6 +44,8 @@ struct {
     moodycamel::BlockingConcurrentQueue<Action> action_queue{};
 } timer_context;
 
+extern std::atomic_bool exited;
+
 uint64_t duration_to_ticks(std::chrono::high_resolution_clock::duration duration) {
     uint64_t delta_micros = std::chrono::duration_cast<std::chrono::microseconds>(duration).count();
     // More accurate than using a floating point timer, will only overflow after running for 12.47 years
@@ -97,7 +99,7 @@ void timer_thread(RDRAM_ARG1) {
         }
     };
 
-    while (true) {
+    while (!exited) {
         // Empty the action queue
         Action cur_action;
         while (timer_context.action_queue.try_dequeue(cur_action)) {
@@ -105,9 +107,15 @@ void timer_thread(RDRAM_ARG1) {
         }
 
         // If there's no timer to act on, wait for one to come in from the action queue
-        while (active_timers.empty()) {
-            timer_context.action_queue.wait_dequeue(cur_action);
-            process_timer_action(cur_action);
+        while (active_timers.empty() && !exited) {
+            using namespace std::chrono_literals;
+            if (timer_context.action_queue.wait_dequeue_timed(cur_action, 1ms)) {
+                process_timer_action(cur_action);
+            }
+        }
+
+        if (exited) {
+            break;
         }
 
         // Get the timer that's closest to running out
@@ -141,8 +149,19 @@ void timer_thread(RDRAM_ARG1) {
 }
 
 void ultramodern::init_timers(RDRAM_ARG1) {
+    start_time = std::chrono::high_resolution_clock::now();
+    ostime_offset = 0;
+
+    Action stale_action{AddTimerAction{NULLPTR}};
+    while (timer_context.action_queue.try_dequeue(stale_action)) {}
+
     timer_context.thread = std::thread{ timer_thread, PASS_RDRAM1 };
-    timer_context.thread.detach();
+}
+
+void ultramodern::join_timer_thread() {
+    if (timer_context.thread.joinable()) {
+        timer_context.thread.join();
+    }
 }
 
 uint32_t ultramodern::get_speed_multiplier() {
