diff --git a/librecomp/include/librecomp/mods.hpp b/librecomp/include/librecomp/mods.hpp
index d7a464e..0c45df7 100644
--- a/librecomp/include/librecomp/mods.hpp
+++ b/librecomp/include/librecomp/mods.hpp
@@ -15,6 +15,7 @@
 #include <variant>
 #include <mutex>
 #include <optional>
+#include <functional>
 
 #include "blockingconcurrentqueue.h"
 
@@ -290,6 +291,11 @@ namespace recomp {
 
         using GenericFunction = std::variant<recomp_func_t*>;
 
+        class ModCodeHandle;
+        struct ModCodeHandleInputs;
+        using ModCodeHandleFactory = std::function<std::unique_ptr<ModCodeHandle>(
+            const N64Recomp::Context&, const ModCodeHandleInputs&)>;
+
         class ModContext;
         class ModHandle;
         using content_enabled_callback = void(ModContext&, const ModHandle&);
@@ -351,6 +357,7 @@ namespace recomp {
 
             void register_game(const std::string& mod_game_id);
             void register_embedded_mod(const std::string& mod_id, std::span<const uint8_t> mod_bytes);
+            void register_static_mod_code(const std::string& mod_id, ModCodeHandleFactory factory);
             std::vector<ModOpenErrorDetails> scan_mod_folder(const std::filesystem::path& mod_folder);
             void close_mods();
             void load_mods_config();
@@ -410,6 +417,7 @@ namespace recomp {
             // Maps game mod ID to the mod's internal integer ID. 
             std::unordered_map<std::string, size_t> mod_game_ids;
             std::unordered_map<std::string, std::span<const uint8_t>> embedded_mod_bytes;
+            std::unordered_map<std::string, ModCodeHandleFactory> static_mod_code_factories;
             std::vector<ModHandle> opened_mods;
             std::unordered_map<std::string, size_t> opened_mods_by_id;
             std::unordered_map<std::filesystem::path::string_type, size_t> opened_mods_by_filename;
@@ -622,6 +630,7 @@ namespace recomp {
 
         void initialize_mods();
         void register_embedded_mod(const std::string &mod_id, std::span<const uint8_t> mod_bytes);
+        void register_static_mod_code(const std::string& mod_id, ModCodeHandleFactory factory);
         void scan_mods();
         void close_mods();
         std::filesystem::path get_mods_directory();
diff --git a/librecomp/src/mods.cpp b/librecomp/src/mods.cpp
index 2cef2ff..430e03a 100644
--- a/librecomp/src/mods.cpp
+++ b/librecomp/src/mods.cpp
@@ -636,6 +636,10 @@ void recomp::mods::ModContext::register_embedded_mod(const std::string &mod_id,
     embedded_mod_bytes.emplace(mod_id, mod_bytes);
 }
 
+void recomp::mods::ModContext::register_static_mod_code(const std::string& mod_id, ModCodeHandleFactory factory) {
+    static_mod_code_factories.insert_or_assign(mod_id, std::move(factory));
+}
+
 void recomp::mods::ModContext::close_mods() {
     std::unique_lock lock(opened_mods_mutex);
     opened_mods_by_id.clear();
@@ -2357,10 +2361,35 @@ recomp::mods::CodeModLoadError recomp::mods::ModContext::load_mod_code(uint8_t*
         .reference_section_addresses = section_addresses,
     };
 
+    auto static_code_it = static_mod_code_factories.find(mod.manifest.mod_id);
+    if (static_code_it != static_mod_code_factories.end()) {
+        fprintf(stderr, "Using statically linked code for mod %s\n", mod.manifest.mod_id.c_str());
+        // Static native code can't be regenerated to add hooks around a
+        // replacement, just like the offline dynamic-library path below.
+        if (!entry_func_hooks.empty() || !return_func_hooks.empty()) {
+            return CodeModLoadError::OfflineModHooked;
+        }
+
+        mod.code_handle = static_code_it->second(*mod.recompiler_context, handle_inputs);
+        if (mod.code_handle == nullptr || !mod.code_handle->good()) {
+            mod.code_handle.reset();
+            error_param = mod.manifest.mod_id;
+            return CodeModLoadError::FailedToLoadNativeCode;
+        }
+
+        cur_error = validate_api_version(mod.code_handle->get_api_version(), cur_error_param);
+        if (cur_error != CodeModLoadError::Good) {
+            error_param = mod.manifest.mod_id;
+            if (!cur_error_param.empty()) {
+                error_param += ":" + std::move(cur_error_param);
+            }
+            return cur_error;
+        }
+    }
     // Use a dynamic library code handle. This feature isn't meant to be used by end users, but provides a more debuggable
     // experience than the live recompiler for mod developers.
     // Enabled if the mod's filename ends with ".offline.nrm".
-    if (mod.manifest.mod_root_path.filename().string().ends_with(".offline.nrm")) {
+    else if (mod.manifest.mod_root_path.filename().string().ends_with(".offline.nrm")) {
         // Hooks can't be generated for native mods, so return an error if any of the functions this mod replaces are also hooked by another mod.
         if (!entry_func_hooks.empty() || !return_func_hooks.empty()) {
             return CodeModLoadError::OfflineModHooked;
diff --git a/librecomp/src/recomp.cpp b/librecomp/src/recomp.cpp
index ea17675..8253910 100644
--- a/librecomp/src/recomp.cpp
+++ b/librecomp/src/recomp.cpp
@@ -95,6 +95,11 @@ void recomp::mods::register_embedded_mod(const std::string &mod_id, std::span<co
     mod_context->register_embedded_mod(mod_id, mod_bytes);
 }
 
+void recomp::mods::register_static_mod_code(const std::string& mod_id, ModCodeHandleFactory factory) {
+    std::lock_guard<std::mutex> lock(mod_context_mutex);
+    mod_context->register_static_mod_code(mod_id, std::move(factory));
+}
+
 void recomp::mods::scan_mods() {
     std::vector<recomp::mods::ModOpenErrorDetails> mod_open_errors;
     {
